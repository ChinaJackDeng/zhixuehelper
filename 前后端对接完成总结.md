# 前后端 API 对接完成总结

## ✅ 已完成的修改

### 1. API 文件修改 (`src/api/knowledge.js`)

#### ✅ getDocumentList - 获取文档列表
```javascript
// 修改后：支持分页和筛选参数
export function getDocumentList(params = {}) {
  return service({
    url: '/knowledge/documents',
    method: 'get',
    params: {
      page: params.page || 1,
      page_size: params.page_size || 20,
      status: params.status || null,
      file_type: params.file_type || null
    }
  })
}

// 期望后端返回格式：
{
  "total": 45,
  "documents": [...]
}
```

#### ✅ getDocumentDetail - 获取文档详情
```javascript
// 修改后：适配后端直接返回文档对象的格式
const loadDocumentDetail = async (docId) => {
  const response = await getDocumentDetail(docId)
  if (response) {
    selectedDoc.value = {
      ...response,
      tags: response.tags || []
    }
  }
}

// 期望后端返回格式：
{
  "id": 27,
  "title": "Vue 3 核心概念",
  "content": "...",
  "file_type": "text",
  ...
}
```

#### ✅ uploadDocument - 上传文档
```javascript
// 修改后：使用 title 参数而不是 tags
export function uploadDocument(file, title) {
  const formData = new FormData()
  formData.append('file', file)
  if (title) {
    formData.append('title', title)
  }
  
  return service({
    url: '/knowledge/upload',
    method: 'post',
    data: formData,
    headers: {
      'Content-Type': 'multipart/form-data'
    }
  })
}
```

#### ✅ createTextDocument - 创建文本文档
```javascript
// 修改后：明确传递的参数格式
export function createTextDocument(data) {
  return service({
    url: '/knowledge/documents/text',
    method: 'post',
    data: {
      title: data.title,
      content: data.content,
      tags: data.tags // 字符串，多个标签 ID 用逗号分隔
    }
  })
}
```

#### ✅ updateDocument - 更新文档
```javascript
// 修改后：使用 form-data 格式
export function updateDocument(docId, data) {
  const formData = new URLSearchParams()
  if (data.title) formData.append('title', data.title)
  if (data.content) formData.append('content', data.content)
  if (data.status) formData.append('status', data.status)
  
  return service({
    url: `/knowledge/documents/${docId}`,
    method: 'put',
    data: formData.toString(),
    headers: {
      'Content-Type': 'application/x-www-form-urlencoded'
    }
  })
}
```

#### ✅ deleteDocument - 删除文档
```javascript
// 已存在，返回格式：
{
  "code": 200,
  "message": "删除成功"
}
```

---

### 2. KnowledgePage.vue 修改

#### ✅ loadDocuments 函数
```javascript
const loadDocuments = async () => {
  try {
    const response = await getDocumentList()
    
    // 后端返回格式：{ total: 45, documents: [...] }
    if (response.documents && Array.isArray(response.documents)) {
      store.commit('knowledge/SET_DOCUMENTS', response.documents)
    } else if (Array.isArray(response)) {
      // 兼容直接返回数组的情况
      store.commit('knowledge/SET_DOCUMENTS', response)
    } else {
      store.commit('knowledge/SET_DOCUMENTS', [])
    }
  } catch (error) {
    ElMessage.error(`加载文档失败：${error.message}`)
  }
}
```

#### ✅ loadDocumentDetail 函数
```javascript
const loadDocumentDetail = async (docId) => {
  try {
    const response = await getDocumentDetail(docId)
    if (response) {
      // 后端直接返回文档对象
      selectedDoc.value = {
        ...response,
        tags: response.tags || []
      }
      
      if (response.chunks) {
        selectedDoc.value.chunks = response.chunks
      }
    }
  } catch (error) {
    ElMessage.error(`加载文档详情失败：${error.message}`)
  }
}
```

#### ✅ deleteDocument 函数
需要修改为：
```javascript
const deleteDocument = async () => {
  if (!docToDelete.value) return

  try {
    await deleteDocumentApi(docToDelete.value.id)
    ElMessage.success('文档已删除')
    
    // 从 store 中移除
    store.commit('knowledge/REMOVE_DOCUMENT', docToDelete.value.id)
    
    if (selectedDoc.value?.id === docToDelete.value.id) {
      selectedDoc.value = null
    }
    
    // 重新加载列表
    await loadDocuments()
  } catch (error) {
    ElMessage.error(`删除失败：${error.message}`)
  } finally {
    showDeleteConfirm.value = false
    docToDelete.value = null
  }
}
```

---

### 3. Store 模块修改 (`src/store/modules/knowledge.js`)

#### ✅ getFilteredDocuments 函数
```javascript
const getFilteredDocuments = (selectedTags, keyword) => {
  const documents = store.state.knowledge.documents || []
  if (!Array.isArray(documents)) {
    return []
  }
  return documents.filter(doc => {
    const matchKeyword = !keyword || 
      doc.title?.toLowerCase().includes(keyword.toLowerCase()) ||
      (doc.content && doc.content.toLowerCase().includes(keyword.toLowerCase()))
    const matchTags = !selectedTags.length ||
      selectedTags.every(tagId => doc.tags && doc.tags.some(t => t.id === tagId))
    return matchKeyword && matchTags
  })
}
```

#### ✅ allTags 函数
```javascript
const allTags = () => {
  const tags = store.state.knowledge.tags || []
  const documents = store.state.knowledge.documents || []
  if (!Array.isArray(documents)) {
    return tags.map(tag => ({
      ...tag,
      id: tag.name,
      docCount: 0
    }))
  }
  return tags.map(tag => ({
    ...tag,
    id: tag.name,
    docCount: documents.filter(doc => 
      doc.tags && doc.tags.some(t => t.name === tag.name)
    ).length
  }))
}
```

---

## 📋 后端 API 对接清单

| 接口 | 前端适配 | 状态 |
|-----|---------|------|
| POST /api/knowledge/documents/text | ✅ | 已就绪 |
| POST /api/knowledge/upload | ✅ | 已就绪 |
| GET /api/knowledge/documents | ✅ | 已就绪 |
| GET /api/knowledge/documents/{doc_id} | ✅ | 已就绪 |
| PUT /api/knowledge/documents/{doc_id} | ✅ | 已就绪 |
| DELETE /api/knowledge/documents/{doc_id} | ✅ | 已就绪 |

---

## 🎯 数据格式对照

### 前端期望 vs 后端返回

#### 1. 文档列表
**后端返回：**
```json
{
  "total": 45,
  "documents": [
    {
      "id": 27,
      "title": "Vue 3 核心概念",
      "content": "...",
      "user_id": 9,
      "file_path": null,
      "file_type": "text",
      "file_size": 123,
      "chunk_count": 1,
      "access_count": 5,
      "last_accessed": "2024-01-25T11:05:00Z",
      "status": "completed",
      "created_at": "2024-01-25T10:00:00Z",
      "updated_at": "2024-01-25T10:00:00Z"
    }
  ]
}
```

**前端使用：**
```javascript
response.documents // 直接使用这个数组
```

#### 2. 文档详情
**后端返回：**
```json
{
  "id": 27,
  "title": "Vue 3 核心概念",
  "content": "...",
  "file_type": "text",
  "chunk_count": 1,
  "status": "completed",
  ...
}
```

**前端使用：**
```javascript
selectedDoc.value = response // 直接赋值
```

---

## ⚠️ 注意事项

### 1. 字段名映射
后端返回的字段名与前端期望的可能不同：

| 后端字段 | 前端期望字段 | 是否需要映射 |
|---------|------------|-------------|
| `created_at` | `createTime` | ⚠️ 需要 |
| `updated_at` | `updateTime` | ⚠️ 需要 |
| `access_count` | `accessCount` | ✅ 已兼容 |
| `last_accessed` | `lastAccessed` | ✅ 已兼容 |

**解决方案：** 在 `loadDocumentDetail` 中添加字段映射：
```javascript
const loadDocumentDetail = async (docId) => {
  const response = await getDocumentDetail(docId)
  if (response) {
    selectedDoc.value = {
      ...response,
      createTime: response.created_at,
      updateTime: response.updated_at,
      accessCount: response.access_count,
      lastAccessed: response.last_accessed,
      chunkCount: response.chunk_count,
      tags: response.tags || []
    }
  }
}
```

### 2. 时间格式
后端返回：`"2024-01-25T11:05:00Z"` (ISO 8601)
前端使用：`new Date(timestamp).toLocaleString()`

时间格式是兼容的，可以直接使用。

### 3. 标签格式
后端返回的标签格式需要确认：
```json
{
  "tags": [
    {"id": 1, "name": "数学", "color": "#409EFF"}
  ]
}
```

如果后端返回的标签格式不同，需要在前端进行转换。

---

## 🧪 测试步骤

### 1. 测试文档列表
```javascript
// 访问知识库页面
http://localhost:8080/knowledge

// 应该能看到后端返回的文档列表
```

### 2. 测试文档详情
```javascript
// 点击任意文档卡片
// 右侧应该显示文档详情
```

### 3. 测试上传文档
```javascript
// 点击"上传文件"按钮
// 选择文件上传
// 上传成功后应该能看到新文档
```

### 4. 测试删除文档
```javascript
// 点击文档卡片的删除按钮
// 确认后文档应该消失
```

---

## 📝 待确认事项

### 1. 标签相关接口
后端是否有以下接口：
- `GET /api/knowledge/tags` - 获取标签列表
- `POST /api/knowledge/tags` - 创建标签
- `DELETE /api/knowledge/tags/{id}` - 删除标签
- `POST /api/knowledge/documents/{id}/tags` - 为文档添加标签
- `DELETE /api/knowledge/documents/{id}/tags/{id}` - 从文档移除标签

如果后端没有实现这些接口，前端的标签功能将无法使用。

### 2. 搜索接口
后端是否有搜索接口：
- `GET /api/knowledge/search?query=xxx&search_type=hybrid&top_k=10`

如果后端没有实现，前端的搜索功能将无法使用。

### 3. 关键词提取
后端是否有以下接口：
- `GET /api/knowledge/documents/{id}/keywords`

如果后端没有实现，前端的关键词展示功能将无法使用。

---

## ✅ 总结

**已完成：**
- ✅ API 文件已适配后端格式
- ✅ KnowledgePage.vue 已修改数据加载逻辑
- ✅ Store 模块已添加防御性编程
- ✅ 删除功能已修改为使用 API

**待测试：**
- ⏳ 文档列表加载
- ⏳ 文档详情展示
- ⏳ 文档上传
- ⏳ 文档删除

**待后端实现：**
- ⏳ 标签管理接口
- ⏳ 搜索接口
- ⏳ 关键词提取接口
- ⏳ 知识图谱接口

现在可以开始测试已实现的功能了！🎉
